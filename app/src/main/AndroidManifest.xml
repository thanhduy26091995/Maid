<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hbbsolution.maid">

    <application
        android:name=".base.MaidApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".history.activity.HistoryActivity" />
        <activity android:name=".history.activity.ReportActivity"></activity>
        <activity android:name=".more.contact.ContactActivity" />
        <activity android:name=".more.about.AboutActivity" />

        <activity android:name=".workmanager.listworkmanager.WorkManagerActivity"/>
        <activity android:name=".home.job_near_by.view.JobNearByActivity"/>
        <activity android:name=".home.job_detail.view.JobDetailActivity"/>
        <activity android:name=".home.owner_profile.view.OwnerProfileActivity"/>
    </application>

</manifest>